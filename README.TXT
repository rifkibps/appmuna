Jika ada error:
 - RuntimeError: Model class backend.models.BackendSubjectsModel doesn't declare an explicit app_label and isn't in an application in INSTALLED_APPS.
Berarti ada packages yang belum keinstall

